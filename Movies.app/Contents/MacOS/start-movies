#!/bin/zsh
set -e

repo_root="$(cd -- "$(dirname -- "$0")/../../.." && pwd)"
cd -- "$repo_root"
export PATH="/opt/homebrew/bin:/usr/local/bin:$PATH"

brew services start postgresql@16 >>desktop/runtime.log 2>&1
exec npm --prefix desktop start >>desktop/runtime.log 2>&1
